const routes = [
  // Public routes
  require("./routes/publicRoutes"),
  // User related routes
  require("./routes/userRoutes"),
  // Post related routes
  require("./routes/postRoutes"),
];
module.exports = routes;
